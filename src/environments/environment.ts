// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
      apiKey: "AIzaSyB79X1b5mo_yfGzWlJlquSWpvdrnFokXC4",
      authDomain: "softwarearchitecturefinal.firebaseapp.com",
      databaseURL: "https://softwarearchitecturefinal.firebaseio.com",
      projectId: "softwarearchitecturefinal",
      storageBucket: "softwarearchitecturefinal.appspot.com",
      messagingSenderId: "287025610969",
      appId: "1:287025610969:web:28bad4dee06f7f4b88bec8"
    }
  };
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/dist/zone-error';  // Included with Angular CLI.